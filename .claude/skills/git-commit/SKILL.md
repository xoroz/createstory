# Git Commit Skill

## Description
Commits changes using the custom gi.sh script with proper commit messages.

## When to Use
- User asks to commit changes
- User mentions "commit" or "save changes to git"
- After completing a task that requires committing

## How to Use
Always use the gi.sh script for commits:
```bash
./gi.sh "descriptive commit message"
